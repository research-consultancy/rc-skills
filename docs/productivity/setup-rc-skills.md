# setup-rc-skills

Run `/setup-rc-skills` once per repository to configure:

- which issue tracker is used (GitHub Issues, Linear, or local files)
- which labels represent triage states for RC workflows
- where generated research notes and docs should be written

This setup skill is intentionally lightweight so other RC medical research skills can assume a consistent project configuration.

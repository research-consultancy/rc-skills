# Issue tracker: GitHub

Issues and PRDs live in GitHub Issues for the repository inferred from `git remote`. Use `gh` to create, read, list, comment on, label, and close issues.

PRs as a request surface: no.

When a skill says “publish to the issue tracker,” create a GitHub issue. When it says “fetch the relevant ticket,” use `gh issue view <number> --comments`.

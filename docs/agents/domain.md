# Domain docs

This is a single-context repository.

Before exploring, read root `CONTEXT.md` and relevant ADRs under `docs/adr/`. If either is absent, proceed silently.

Use the glossary’s canonical vocabulary. Surface conflicts with existing ADRs explicitly rather than silently overriding them.
